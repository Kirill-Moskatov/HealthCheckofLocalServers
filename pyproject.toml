[project]
name = "pulse"
version = "0.1.0"
description = "Pulse — личный дашборд healthcheck домашних сервисов"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.0",
    "aiosqlite>=0.20.0",
    "pydantic-settings>=2.0.0",
    "httpx>=0.27.0",
]

[tool.uvicorn]
host = "0.0.0.0"
port = 8000
